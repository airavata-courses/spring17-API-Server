global
    daemon
    maxconn 12

defaults
    mode default
    timeout 5

listen http-in
    bind *:9000{{range service "API-Server"}}
    server {{.Node}} {{.Address}}:{{.Port}}{{end}}

listen stats *:8936
    stats enable
    stats uri /
    stats hide-version
    stats auth someuser:password
