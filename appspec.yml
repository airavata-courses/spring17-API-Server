version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/
    overwrite: yes
hooks:
  AfterInstall:
    - location: build-scripts/codeDeployInstall.sh
      timeout: 300
runas: root
